# Base URL of the ApplyFlow API server
VITE_API_BASE=http://localhost:8000

# Base URL of the ApplyFlow web application
# Must match the URL the user has the web app open at (for SSO auth-bridge)
VITE_WEB_BASE=http://localhost:3000
